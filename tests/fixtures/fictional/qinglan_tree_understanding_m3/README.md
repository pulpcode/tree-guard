# 青岚信息树理解 M3 独立覆盖层

本目录只补充现有 312 节点完全虚构树无法从当前结构自行推导的
`NEW_NODE_PLACEMENT` 语义种子。它不修改三个已冻结的青岚基础 fixture，也不包含
自然语言需求、预期状态、Oracle、人工审批或模型输出。

生成路径只能在本地校验来源快照、父节点和新名称后，把种子转换为
`NewNodePlacementSeed`。模型只能看到单次投影的临时节点引用和这个虚构新字段的
允许列表信息。原有 `scenarios.json`、覆盖矩阵与 promotion 文件仍只能在候选生成
完成后作为隐藏参照读取。
